{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/home.css') }}">
{% endblock %}

{% block title %}DuelistDepot{% endblock %}

{% block body %}
    <div class="card bg-transparent text-transparent">
        <div class="loader">
            <p>{{ "Tout l'univers Yu-Gi-Oh en un seul endroit"|trans }}</p>
            <div class="words text-indigo-600">
                <span class="word">{{ "cartes"|trans }}</span>
                <span class="word">{{ "boosters"|trans }}</span>
                <span class="word">{{ "tapis"|trans }}</span>
                <span class="word">{{ "deck box"|trans }}</span>
                <span class="word">{{ "protege-cartes"|trans }}</span>
            </div>
        </div>
    </div>
{% endblock %}